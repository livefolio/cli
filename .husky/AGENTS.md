<!-- Parent: ../AGENTS.md -->
<!-- Generated: 2026-04-01 | Updated: 2026-04-01 -->

# .husky

## Purpose
Git hook configuration managed by Husky. Runs `lint-staged` on pre-commit to enforce code quality before every commit.

## Key Files

| File | Description |
|------|-------------|
| `pre-commit` | Git pre-commit hook — runs `npx lint-staged` (ESLint + Prettier on staged `*.ts` files) |

## Subdirectories

| Directory | Purpose |
|-----------|---------|
| `_/` | Husky internal runtime (generated — do not modify) |

## For AI Agents

### Working In This Directory
- Do not modify files in `_/` — they are auto-generated by Husky
- The `pre-commit` hook runs automatically on `git commit`; do not bypass with `--no-verify`
- To change what runs on commit, edit `pre-commit` or the `lint-staged` config in `package.json`

<!-- MANUAL: -->
